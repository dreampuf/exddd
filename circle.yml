machine:
  environment:
    NOTETON_ENV: dev
  services:
    - docker

dependencies:
  pre:
    - docker run --rm -it -v "$PWD":/usr/src/myapp -e GOPATH=/usr/src/myapp -w /usr/src/myapp golang:latest bash -c "go get -d && ./web/bin/revel package web"
